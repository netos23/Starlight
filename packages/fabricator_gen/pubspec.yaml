name: fabricator_gen
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: '>=3.2.5 <4.0.0'

dependencies:
  build: ">=2.0.0 <3.0.0"
  source_gen: ^1.2.7
  fabricator:
    path: ../fabricator

dev_dependencies:
  build_test: ^2.1.7
  build_runner: ">=2.0.0 <3.0.0"
